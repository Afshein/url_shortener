services:
  web:
    build: .
    ports:
    # APP_PORT : CONTAINER_PORT
      - "${APP_PORT}${PORT}"
    environment:
      - PORT={$PORT}
    command: ["/app/server.exe", "-PORT=${PORT}"]


  redis:
    image: redis:latest
    ports:
      - "6379:6379"